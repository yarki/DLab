mocked: false

mongo:
  host: localhost
  port: 27017
  username: admin
  password: XS3ms9R3tP
  database: dlabdb

securityService:
  protocol: http
  host: localhost
  port: 8090
  jerseyClient:
    timeout: 10s
    connectionTimeout: 10s

provisioningService:
  protocol: http
  host: localhost
  port: 8084
  jerseyClient:
    timeout: 3s
    connectionTimeout: 3s

server:
  rootPath: "/api"
  applicationConnectors:
    - type: http
      port: 8080
  adminConnectors:
    - type: http
      port: 8081

logging:
  level: INFO
  loggers:
    com.epam: DEBUG
  appenders:
#    - type: console
    - type: file
      currentLogFilename: /var/opt/dlab/log/ssn/selfservice.log
      archive: true
      archivedLogFilenamePattern: /var/opt/dlab/log/ssn/selfservice-%d{yyyy-MM-dd}.log.gz
      archivedFileCount: 10
